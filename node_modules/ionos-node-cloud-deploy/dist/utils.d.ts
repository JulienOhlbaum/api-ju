import { NodeSSH, SSHExecCommandResponse } from 'node-ssh';
import { FtpDeployConfig } from './types';
export declare function uploadSftp(config: FtpDeployConfig): Promise<void>;
export declare const execRestart: (ssh: NodeSSH, dir: string) => Promise<SSHExecCommandResponse>;
