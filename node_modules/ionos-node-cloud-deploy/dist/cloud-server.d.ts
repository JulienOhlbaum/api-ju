import { Config } from 'node-ssh';
import { DeployConfig, FtpDeployConfig } from './types';
interface CloudServerOptions {
    host?: string;
    username?: string;
    password?: string;
    localDir?: string;
    remoteDir?: string;
    include?: string[];
    exclude?: string[];
}
export default class CloudServer {
    remoteDir: string;
    ftpConfig: FtpDeployConfig;
    sshConfig: Config;
    constructor(options?: CloudServerOptions);
    private sshConnect;
    upload(options?: DeployConfig): Promise<void>;
    install(): Promise<void>;
    restart(): Promise<void>;
    deploy(options?: DeployConfig): Promise<void>;
}
export {};
